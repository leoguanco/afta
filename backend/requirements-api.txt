# API Service Dependencies (Minimal)
# Core API framework
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Task Queue (API only enqueues tasks, doesn't execute)
celery>=5.3.0
redis>=5.0.0

# Observability
prometheus-client>=0.19.0

# Dependencies required for some Use Case imports (even if logic is minimal)
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0
statsbombpy>=1.13.0
minio>=7.2.0

# AI/ML Stack (Required for Streaming API)
crewai
langchain_openai
langchain-google-genai

# RAG (Required for indexing API endpoints)
chromadb
sentence-transformers
